name: git action 공부

on: [pull_request]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 기여자의 브랜치로 checkout
        uses: actions/checkout@v3
        with:
          ref: ${{ github.head_ref }}

      - name: node 설정
        uses: actions/setup-node@v3
        with:
          node-version: 20.x
          cache: "yarn"

      - name: 변경된 .ts, .tsx 파일 가져오기
        id: changed-files
        uses: tj-actions/changed-files@v45
        with:
          files: |
            **.ts 
            **.tsx

      - name: TS_CHANGED_FILES 확인
        if: steps.changed-markdown-files.outputs.any_changed == 'true'
        env:
          TS_CHANGED_FILES: ${{ steps.changed-markdown-files.outputs.all_changed_files }}
        run: |
          for file in ${TS_CHANGED_FILES}; do
            echo "$file was changed"
          done

      # - name: 변경된 파일 변수에 담기
      #   env:
      #     ALL_CHANGED_FILES: ${{ steps.changed-files.outputs.all_changed_files }}
      #   run: echo "변경된 파일 리스트 ${{ env.ALL_CHANGED_FILES }}"

      # - name: asd
      #   run: |
      #     TS_CHANGED_FILES=$(echo "${{ steps.changed-files.outputs.all_changed_files }}" | tr ' ' '\n' | grep -E '\.ts$|\.tsx$' | tr '\n' ' ')
      #     echo "TS 파일 리스트: $TS_CHANGED_FILES"
      #     echo "TS_CHANGED_FILES=$TS_CHANGED_FILES" >> $GITHUB_ENV
